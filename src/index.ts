import { findBestMatch } from './matcher.js';
import faqs from '../faqs.json' assert { type: 'json' };

// TODO: implement interactive CLI
console.log('Riverside Books Chatbot — work in progress');
console.log(`Loaded ${faqs.length} FAQs`);
